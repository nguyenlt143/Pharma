<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console output for general test logs -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for Owner test evidence -->
    <appender name="OWNER_EVIDENCE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/owner/integration-test.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/owner/integration-test-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!-- Special logger for Owner test evidence -->
    <logger name="OWNER_EVIDENCE" level="INFO" additivity="false">
        <appender-ref ref="OWNER_EVIDENCE_FILE"/>
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Reduce Spring Boot startup noise during tests -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
</configuration>

