<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console output for general test logs -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for Admin test evidence -->
    <appender name="ADMIN_EVIDENCE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/admin/integration-test.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/admin/integration-test-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!-- Special logger for Admin test evidence -->
    <logger name="ADMIN_EVIDENCE" level="INFO" additivity="false">
        <appender-ref ref="ADMIN_EVIDENCE_FILE"/>
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Reduce Spring Boot startup noise during tests -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
</configuration>

